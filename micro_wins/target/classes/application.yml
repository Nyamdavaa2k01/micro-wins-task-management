spring:
  datasource:
    url: jdbc:hsqldb:mem:testDB
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true